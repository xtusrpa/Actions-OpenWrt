#
# https://github.com/P3TERX/Actions-OpenWrt
#
# File: .github/workflows/istoreos-test.yml
# Description: Build iStoreOS using GitHub Actions
#
# Copyright (c) 2019-2024 P3TERX <https://p3terx.com>
#
# This is free software, licensed under the MIT License.
# See /LICENSE for more information.
#

name: iStoreOS 固件编译流水线

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      ssh:
        description: 'SSH connection to Actions'
        required: false
        default: 'true'
        type: boolean

# 显式赋予流水线完整的写入权限，确保清理旧 Release、旧运行记录和 Git Tags 时不会触发 403 错误
permissions:
  contents: write
  actions: write

env:
  REPO_URL: https://github.com/istoreos/istoreos
  REPO_BRANCH: istoreos-24.10
  FEEDS_CONF: feeds.conf.default
  CONFIG_FILE: istoreos.config
  DIY_P1_SH: diy-part1.sh
  DIY_P2_SH: diy-istoreos.sh
  UPLOAD_BIN_DIR: false
  UPLOAD_FIRMWARE: true
  UPLOAD_RELEASE: true
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    # [作用说明] 检出（拉取）当前 GitHub 仓库的代码到运行环境中。
    - name: Checkout
      uses: actions/checkout@v4

    # [作用说明] 释放 Ubuntu 运行环境的磁盘空间，清理多余的大型软件包，防止编译 iStoreOS 时因磁盘空间不足而报错。
    - name: Free Disk Space (Ubuntu)
      uses: jlumbroso/free-disk-space@main
      with:
        tool-cache: false
        android: true
        dotnet: true
        haskell: true
        large-packages: true
        docker-images: true
        swap-storage: true

    # [作用说明] 初始化编译环境，安装编译 OpenWrt/iStoreOS 固件所需的所有底层依赖包（如 gcc, git, ccache 等）。
    - name: Initialization environment
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo rm -rf /etc/apt/sources.list.d/* /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
        sudo docker image prune --all --force
        sudo -E apt-get -qq update
        sudo -E apt-get -qq install ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential bzip2 ccache cmake cpio curl device-tree-compiler fastjar flex gawk gettext gcc-multilib g++-multilib git gperf haveged help2man intltool libc6-dev-i386 libelf-dev libfuse-dev libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libpython3-dev libreadline-dev libssl-dev libtool lrzsz mkisofs msmtp ninja-build p7zip p7zip-full patch pkgconf python2.7 python3 python3-pyelftools python3-setuptools qemu-utils rsync scons squashfs-tools subversion swig texinfo uglifyjs upx-ucl unzip vim wget xmlto xxd zlib1g-dev
        sudo -E apt-get -qq autoremove --purge
        sudo -E apt-get -qq clean
        sudo timedatectl set-timezone "$TZ"
        sudo mkdir -p /workdir
        sudo chown $USER:$GROUPS /workdir

    # [作用说明] 从官方 GitHub 仓库克隆 iStoreOS 的基础源码到工作目录中。
    - name: Clone source code
      working-directory: /workdir
      run: |
        df -hT $PWD
        git clone $REPO_URL -b $REPO_BRANCH openwrt
        ln -sf /workdir/openwrt $GITHUB_WORKSPACE/openwrt

    # [作用说明] 加载您可能自定义的 feeds 配置文件，并执行第一部分自定义脚本 (diy-part1.sh)，通常用于添加第三方插件库（如 helloworld）。
    - name: Load custom feeds
      run: |
        [ -e $FEEDS_CONF ] && mv $FEEDS_CONF openwrt/feeds.conf.default
        chmod +x $DIY_P1_SH
        cd openwrt
        $GITHUB_WORKSPACE/$DIY_P1_SH

    # [作用说明] 更新所有 feeds 源（拉取所有插件源码的最新的列表）。
    - name: Update feeds
      run: cd openwrt && ./scripts/feeds update -a

    # [作用说明] 替换并升级编译环境中的 Go 语言版本到 1.24.x，解决最新版 mosdns、OpenClash 等插件因 Go 版本过低(1.23)导致的编译失败。
    - name: Upgrade Go to 1.24.x
      run: |
        cd openwrt
        rm -rf feeds/packages/lang/golang
        git clone https://github.com/sbwml/packages_lang_golang -b 24.x feeds/packages/lang/golang

    # [作用说明] 将 feeds 中更新好的源码链接到系统内部，准备后续正式编译。
    - name: Install feeds
      run: cd openwrt && ./scripts/feeds install -a

    # [作用说明] 加载自定义的编译配置文件 (.config)，应用预设的路由器 IP 或密码设置，并执行第二部分自定义脚本 (diy-istoreos.sh)。
    - name: Load custom configuration
      run: |
        [ -e files ] && mv files openwrt/files
        [ -e $CONFIG_FILE ] && mv $CONFIG_FILE openwrt/.config
        chmod +x $DIY_P2_SH
        cd openwrt
        $GITHUB_WORKSPACE/$DIY_P2_SH

    # [作用说明] 提供 tmate 的 SSH 后门接入功能。如果在触发流水线时勾选了 SSH 选项，编译到这里会暂停，让您通过终端连入服务器进行手动调试。
    - name: SSH connection to Actions
      uses: mxschmitt/action-tmate@v3
      if: (github.event.inputs.ssh == 'true' && github.event.inputs.ssh != 'false') || contains(github.event.action, 'ssh')
        
    # [作用说明] 提前下载编译所需的所有文件（dl目录中的压缩包）。这一步可以显著减少正式编译时的网络报错，并加快多线程编译速度。
    - name: Download package
      id: package
      run: |
        cd openwrt
        make defconfig
        make download -j8
        find dl -size -1024c -exec ls -l {} \;
        find dl -size -1024c -exec rm -f {} \;

    # [作用说明] 最核心的环节：调用系统所有 CPU 核心（多线程）开始编译固件。如果多线程失败，则会自动降级为单线程 (make -j1) 打印详细错误以排查问题。
    - name: Compile the firmware
      id: compile
      run: |
        cd openwrt
        echo -e "$(nproc) thread compile"
        make -j$(nproc) || make -j1 || make -j1 V=s
        echo "status=success" >> $GITHUB_OUTPUT
        grep '^CONFIG_TARGET.*DEVICE.*=y' .config | sed -r 's/.*DEVICE_(.*)=y/\1/' > DEVICE_NAME
        [ -s DEVICE_NAME ] && echo "DEVICE_NAME=_$(cat DEVICE_NAME)" >> $GITHUB_ENV
        echo "FILE_DATE=_$(date +"%Y%m%d%H%M")" >> $GITHUB_ENV

    # [作用说明] 在编译完成后，输出一下当前机器的磁盘剩余空间，方便遇到空间不够报错时进行诊断。
    - name: Check space usage
      if: (!cancelled())
      run: df -hT

    # [作用说明] （根据顶部设置 UPLOAD_BIN_DIR 决定是否执行）如果开启，则将包含了所有生成的 ipk 安装包的整个 bin 目录上传到 Action 附件中。
    - name: Upload bin directory
      uses: actions/upload-artifact@v4
      if: steps.compile.outputs.status == 'success' && env.UPLOAD_BIN_DIR == 'true'
      with:
        name: iStoreOS_bin${{ env.DEVICE_NAME }}${{ env.FILE_DATE }}
        path: openwrt/bin

    # [作用说明] 剔除临时生成的多余 ipk 包文件，单独把我们需要刷机使用的核心固件（img 等格式）整理提取出来，缩小上传体积。
    - name: Organize files
      id: organize
      if: steps.compile.outputs.status == 'success' && env.UPLOAD_FIRMWARE == 'true' && !cancelled()
      run: |
        cd openwrt/bin/targets/*/*
        rm -rf packages
        echo "FIRMWARE=$PWD" >> $GITHUB_ENV
        echo "status=success" >> $GITHUB_OUTPUT

    # [作用说明] 将整理好的最终固件打包，并上传到当前流水线页面下方的 Artifacts (附件) 中供直接下载。
    - name: Upload firmware directory
      uses: actions/upload-artifact@v4
      if: steps.organize.outputs.status == 'success' && !cancelled()
      with:
        name: iStoreOS_firmware${{ env.DEVICE_NAME }}${{ env.FILE_DATE }}
        path: ${{ env.FIRMWARE }}

    # [作用说明] 使用当前的时间戳生成一个唯一的版本号标签 (Tag)，例如 iStoreOS-2026.06.19-2100，并创建一个 release.txt 占位文件。
    - name: Generate release tag
      id: tag
      if: steps.compile.outputs.status == 'success' && env.UPLOAD_RELEASE == 'true' && !cancelled()
      run: |
        echo "release_tag=iStoreOS-$(date +"%Y.%m.%d-%H%M")" >> $GITHUB_OUTPUT
        touch release.txt
        echo "status=success" >> $GITHUB_OUTPUT

    # [作用说明] 将固件推送发布到该 GitHub 仓库的 Releases 页面（
